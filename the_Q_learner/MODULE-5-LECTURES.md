#### MODULE 5 GOALS
#### INTRO

Welcome to Module 5. This week, we are making a big shift, moving from planning with a complete map to learning through raw interaction. Imagine trying to design an AI for a complex task, but you can't give it the rules, the probabilities, or even a basic layout of its environment. This is where model-free reinforcement learning comes in. We will start by discussing Q-Learning, which allows agents to build their strategies directly from the feedback they get by interacting with the world. We will also look at the classic exploration versus exploitation dilemma. We will discuss how to balance getting immediate rewards with exploring new options, using guided strategies like softmax and upper confidence bounds to keep agents moving forward. Next, we will cover policy gradients, a method where an agent optimizes its behavior directly without relying on a secondary value function. This approach is essentially useful for continuous action spaces, like those we see in robotics. Finally, we will bring these ideas together by looking at actor-critic architectures, which combine an actor to shape behavior and a critic to evaluate it. Your goal this week is to understand how to balance learning efficiency with system stability. Let's look at what happens when agents learn by doing. 

- Explain the principles of model-free reinforcement learning, including Q-learning, exploration strategies, and policy optimization.
- Apply reinforcement learning concepts to analyze how agents learn from experience and balance exploration with exploitation.
- Evaluate the strengths and tradeoffs of approaches such as Q-learning, policy gradients, and actor-critic architectures for solving different types of reinforcement learning problems.

#### LECTURE1

Welcome to the first video of our lecture on Q-Learning. Today, we will begin our exploration of how agents can learn to act optimally in an environment, even when they don't have access to its internal dynamics. This model -free setting is where Q-Learning shines. We will introduce the idea of Q-Values and walk through the Q -Learning updates step -by-step using a small working example. To understand where Q-learning fits into the bigger picture of reinforcement learning, let's quickly contrast it with the model -based approaches we've already covered. In previous lectures, we assumed the agent knew the transition probabilities and reward function, a luxury often unavailable in real-world settings. Model-free methods like Q-learning allow the agent to learn from interaction by trial, error, and adjustment without needing to know the underlying MDP model. So in this model -free setting, what exactly are we trying to learn? That brings us to the concept of Q -values, the foundation of Q-learning. Instead of learning values of states, Q -learning learns values of state-action pairs. These are called Q-values. Once we have Q star, the optimal action value function, the optimal action in any state, S, is simply the one with the highest Q value. This makes it ideal for online learning and reactive systems. Let's take a closer look at how we actually update these values during learning. Here is the heart of Q -learning, the update rule. After taking action AT in state ST, the agent receives a reward and lands in a new state, ST plus 1. The agent updates its Q value by blending what it previously believed with what it just learned. The term inside the brackets is called the temporal difference, or TD error, the difference between the new target and the old estimate. To better understand this update rule, let's zoom in on this important piece of the equation. The temporal difference error is a signal that tells the agent how surprised it was. It compares the expected target value, that is, the sum of the Q value of the best action possible and the observed reward, against the current Q value of a particular state and action. If the outcome is better than expected, then the current Q value increases. If the outcome is worse, it decreases. This means that, over time, this feedback gradually corrects our estimates of how good actions are. Let's now ground this in a concrete setting. We'll introduce a simple grid world environment where we can watch Q-learning in action. The agent starts in the top left cell, and it must reach the goal in the bottom right. each movement incurs a small penalty say minus one to encourage efficiency one cell acts as a trap and gives a heavy negative reward if it stepped on this setup will let us visualize how q values evolve through exploration and how the agent learns to prefer certain paths over others The queue table starts off blank, all state action values are zero, and will get updated based on the agent's experience. With our environment defined, let's walk through a specific step where the agent takes an action and updates one of its queue values. Imagine the agent is in state 1,1 and chooses to move right. It receives a small negative reward for the step. Let's say the highest Q value for the resulting state 1,2 is currently at 3. Plugging these values into our Q -learning formula with a learning rate alpha equal to 0.1 and a discount factor equal to 0.9, we compute the temporal difference and apply the update. The Q value of the 1,1 state with the write action improves from 0 to 0.17. Over time, these small adjustments push the Q values toward accurate estimates of future reward. Now imagine doing this kind of update over and over again. What happens to our Q values? Let's see how they behave over time. Here we can see how Q values evolve over training episodes. In the beginning, all QValues are zero. The agent basically knows nothing. After a few episodes, you start seeing non -zero estimates in some state action pairs, indicating experiences being incorporated. As learning progresses, QValues for optimal paths increase, while those for suboptimal or dangerous actions remain low or even negative. Ultimately, the agent follows the path with the highest cumulative Q values, which approximates the optimal policy. Of course, this convergence doesn't happen by magic. Certain conditions need to be satisfied for Q learning to work reliably. Let's briefly touch on those. Q-learning is guaranteed to converge under certain conditions. First, the learning rate should decay slowly enough to allow convergence, but not so fast that learning stalls. Second, the agent must visit each state action pair infinitely often, meaning it must keep exploring. Third, the environment must be Markovian, that is, the next state depends only on the current state and action. In practice, we use greedy exploration and fixed or decaying learning rates to get reliable convergence even in noisy environments. With these conditions met, QValues stabilize and the agent behaves optimally. That brings us to the end of our overview of QLearning's value update process. Let's wrap up. We introduced the core of QLearning, value updates through experience. But we haven't yet said how the agent chooses actions during learning. Should it always pick the best option? Should it explore new ones? In the next video, we will tackle this fundamental trade-off, exploration versus exploitation. 

#### LECTURE2

Welcome back. In our last discussion, we introduced Q -learning as a powerful, model-free algorithm for learning an optimal policy in a markup decision process. But a critical question remains. How should the agent use the Q values to actually choose its actions while it's still learning? In this video, we will dive into one of the most critical challenges in reinforcement learning, the exploration versus exploitation dilemma. How does an agent decide whether to take the best-known action or explore something new that might be even better? In reinforcement learning, exploitation means choosing the best -known action, that is, the one that currently gives the highest reward. The agent acts greedily. At any given time step T, when it's in state ST, it looks at its Q table and chooses the action A that it currently believes to yield the highest future reward. Mathematically, it chooses the action that is the argument or argmax of the Q function for that state. This is the quote -unquote safe bet. It's leveraging its current knowledge for the best immediate outcome. Exploration, on the other hand, means trying something new, even if it might not seem optimal right now. The agent does this not for immediate gain, but to gather new information. By trying a quote -unquote sub-optimal action, it might receive an unexpected reward, or it might transition to a valuable new state, allowing it to update its QTable and improve its estimates for the future. Without exploration, our agent's QTable would never improve for actions it has never taken. Finding the right balance between these two is crucial. Too much exploitation leads to local optima. Too much exploration slows down convergence and wastes time. Let's look at a real-world analogy to solidify this idea. before we dive into the technical details let's start with an analogy we all face imagine it's friday night and you're deciding where to eat you found a great burger place and rated it with five stars but nearby is a new thai restaurant with no reviews Should you stick to what you know or explore the new option that might be even better? I must confess that I personally choose exploitation most of the time. I just like what I like, you know? Nonetheless, this is the essence of the exploration versus exploitation dilemma. In reinforcement learning, we use formal strategies to manage this trade -off. Let's begin with the simplest one. The epsilon greedy strategy is incredibly simple but effective. We introduce a parameter, epsilon, which represents the probability that our agent will explore. So, at each step, with probability epsilon, the agent picks a random action, that is, it's exploring. The rest of the time, with probability 1 minus epsilon, it picks the best known action, so it picks exploitation. A small epsilon like 0.1 ensures the agent explores 10% of the time. The other 90% of the time, the agent behaves greedily and exploits. However, an important point to consider is whether the epsilon value should remain fixed throughout the training process. After all, initially the agent does not know much about the world, and it would benefit from experimenting more. This begs the question, how can we adjust the epsilon value as training progresses? Epsilon greedy is great, but we can make it even smarter. The simplest, most straightforward way to go about it is to reduce the value of epsilon as training progresses. Think about the learning process. At the very beginning, the agent's Q table is essentially useless. It's filled with zeros or random values. At this stage, acting greedily doesn't really make much sense. Initially, we want the agent to be exploring as much as possible to build a basic understanding of the world. So, we start with a high value for epsilon, like 1, for example. Remember that an epsilon value of 1 means that the agent is always exploring, always picking random actions without regard of the current Q values. In contrast, after thousands of episodes, the agent has explored a lot and its Q values are becoming quite accurate. it. It now has a good idea of what the optimal policy is. At this point, constant random exploration can be wasteful and hurt performance. As such, as training progresses, we anneal or decay epsilon. That is, we gradually reduce it so that the agent focuses more on exploiting what it has already learned. This balances learning about the environment early on and refining a strong policy later. This is a very common and powerful technique that almost always outperforms a fixed epsilon value. So far, we've looked at random exploration, but it should be evident that it comes with limitations. Now, don't get me wrong. Random exploration works, but not always efficiently. In environments with sparse rewards or narrow optimal paths, random exploration wastes time. The agent might take forever to discover the rewarding path. This motivates the question, what if we could explore more intelligently? But before we can talk about more advanced exploration strategies, let's quickly recap what we've covered this video. We started by defining the fundamental exploration versus exploitation dilemma. The trade-off between sticking with what works and trying something new to find a potentially better option. We saw that a purely greedy agent that only ever exploits is at high risk of getting stuck in a local optimum, learning a policy that is good, but not great. To solve this, we introduced the epsilon greedy strategy, a simple rule that forces the agent to explore with a small probability, epsilon, and exploit the rest of the time. Finally, we discussed a key improvement, decaying the epsilon value over time. This allows the agent to adapt its strategy, exploring a lot when it's ignorant, and then focusing on exploitation once it has built a confident model of the world. In our next video, we will discuss more advanced exploration strategies, such as softmax exploration and the upper confidence bound. See you there. 

#### LECTURE3

Welcome back. In our last video, we introduced the fundamental exploration versus exploitation dilemma and discussed our first strategy for balancing it, the epsilon greedy algorithm. Today, we go beyond this basic strategy and explore a more sophisticated method for exploration. We will start with a powerful technique, the softmax strategy, which explores in proportion to an action's estimated value. This technique helps agents explore efficiently rather than randomly. Let's get started. So, let's quickly formalize the problem with epsilon greedy. Imagine our agent is in a state and has four possible actions with learned Q values of 10, 8, 7, and minus 50. The greedy action is the one with a Q value of 10. However, if our agent decides to explore, epsilon greedy would choose between the actions with values 8, 7, and minus 50 with equal probability. This doesn't seem right, does it? But the action with a value of 8 is clearly more promising than the one with a value of minus 50, yet they get the same chance of being picked. Basically, we're throwing away valuable information. A smarter strategy would instead explore proportionally. It would recognize that the action with a Q value of 8 is worth trying, that the action with a value of 7 is a maybe, and that the action with a value of minus 50 should probably be avoided, unless we're extremely uncertain about it. This is the core idea behind our first new strategy, Softmax Exploration. The softmax strategy directly addresses this by converting our raw q values into a probability distribution. The probability of choosing action a in a given state s is calculated using the softmax function, which you might recognize from classification tasks in machine learning. It takes the exponential of each Q value and then normalizes it by dividing it by the sum of the exponentials for all possible actions. The key parameter here is tau, the temperature. Think of it like a **** that controls how random our decisions are. When the temperature is high, the exponential terms for all actions become closer to 1, making the probabilities nearly uniform. This leads to a lot of exploration. Conversely, when the temperature is very low, even small differences in Q values get magnified by the exponential function. The action with the highest Q value will have a probability close to 1, making the agent behave very greedily. Let's see this in action with a concrete example. Let's work through the numbers. Suppose we have three actions, or arms, of a slot machine, with estimated Q values of 3, 1, and 1.5. As such, action one is our current best guess. First, let's use a high temperature. Let's say tau equals to 2.0. We plug the values into our formula and the exponential of 3 divided by 2 is about 4.48. For action 2, it's 1.65, and for action 3, it's 2.12. After normalizing, we see that action 1 gets a 54% chance of being chosen. But notice that action three, our second best option, still gets a healthy 26% chance. And even the worst action gets a 20% chance. Now, let's crank the temperature down to 0.5. The exponential of 3 divided by 0.5 is now e to the 6, which is a much larger number, 403.4. The other values are now comparatively tiny. after normalizing the probability of choosing our best action action one skyrockets to 94% the other actions are now highly unlikely to be chosen this is the agent focusing its bets behaving much more greedily. In practice, just like with epsilon greedy, you might anneal the temperature over time, starting high and gradually lowering it as the agent becomes more confident on its Q values. So, to wrap up our discussion on softmax exploration, the core idea is that we are choosing actions based on their calculated Q values, making it a much smarter way to explore than Epsilon Gritty. It's all about proportional exploration. We give more promising actions a higher chance, but still keep all options open. And finally, remember the temperature parameter tau. This is our control ****. We can start with a high temperature for broad exploration and then gradually lower it to focus on exploitation as our agent gains confidence. Next up, we will dive into Upper Confidence Bound, which uses the principle of optimism in the face of uncertainty to guide its exploration. See you there. 

#### LECTURE4 

Welcome back. In our previous lecture, we discussed the Softmax strategy for exploration. Softmax explores in proportion to an action's estimated value. Today, we move to a more sophisticated method, the upper confidence bound, or UCB. UCB employs the powerful principle of optimism in the face of uncertainty to guide its exploration. This technique helps our agents explore more efficiently, rather than simple random choices. Let's get started. Now, let's switch gears to a different but equally powerful philosophy of exploration. Upper Confidence Bound, or UCB. While Softmax balances exploration and exploitation probabilistically, UCB does it deterministically by being explicitly optimistic. The guiding principle is optimism in the face of uncertainty. Upper Confidence Bound calculates a score for each action and then greedily picks the action with the highest score. But this score isn't just the Q value. As you can see in the formula, it's the sum of two terms. The first term, QA, it's the exploitation part. The second term is an exploration bonus. This bonus is what makes UCB so clever. Let's break it down. It's proportional to the square root of the natural log of n. n is the total number of actions we've taken, divided by nA, that is, the number of times that we've chosen action A. The term C is a constant that lets us tune how much we value exploration. The key intuition is this. The more we've taken an action that is a large value for N-A, the smaller the exploration bonus becomes. But if we haven't taken an action in a while, that is, N-A is small, N, in total, keeps growing, and the exploration bonus will grow, making it increasingly likely that we will eventually pick that action again to reduce our uncertainty about it. This graph illustrates the behavior of the exploration bonus term. On the x-axis, we have the number of times we've selected a particular action, and on the y -axis is the size of its exploration bonus. When an action has been selected very few times, as shown on the left, Our uncertainty about its true value is high, so UCB gives it a large bonus, encouraging us to pick it. As we select that action more and more, moving to the right on the graph, our estimate of its Q value becomes more reliable. Our uncertainty decreases, and so does the expiration bonus. Eventually, this bonus term will become so small that the action will only be selected if its underlying Q value, the exploitation term, is genuinely the highest. UCB thus, naturally and elegantly, shifts from exploration to exploitation. Let's trace a few steps of UCB to see how this works. We have a three-armed bandit, and we'll set our exploration constant to 1. Initially, we haven't tried any actions, so all counts for NA are 0. Division by 0 means the UCB score for every action is infinite, so we simply try each action once to get a baseline. line. Say we pick action 1 first and we get a reward of 2. We update its Q value to 2 and its count n1 to 1. At this point, actions 2 and 3 still have infinite UCB scores, so we must pick one of them next. Say we pick action 2 next and get a reward of 1. Its Q value is now 1 and it's count and 2 is 1 as well. The bonus term for both action 1 and 2 is the square root of the natural logarithm of 2 over 1 which is about 0.83. Action 3 is the only one left with an infinite score. Thus we pick action three and get a reward of one. Now all actions have been tried at least once and the total number of steps n is three. We can finally calculate the real UCB scores. For action one the score is its Q value of two plus the bonus term the square root of the natural logarithm of 3 over 1, meaning that its UCB score is about 3.05. For actions 2 and 3, their Q values are both 1, so their UCB scores are lower at 2.05. Our agent will now greedily pick action 1 because it has the highest UCB score. So, we pick action 1 again and get a reward of 3. Its count N1 becomes 2, and we average the new reward with the old, so its Q value is 2.5. Now, we recalculate. The total step count N is 4. The UCB score for action 1 is now 2.5 plus the square root of the natural logarithm of 4 over 2, which is about 3.3. Notice that its exploration bonus has shrunk because we just picked it. The bonuses for actions 2 and 3 have grown because the total n increased while their individuals and a did not. so still action one has the highest score so we pick it again for step five you can see the dynamic here the agent will keep exploiting the best looking option but as it does its uncertainty bonus shrinks while the bonuses for the other actions grow inevitably leading to the agent to re-explore them This guarantees that no action is starved of attention forever. Now, you might be wondering, how do we choose between Softmax and UCB? Well, here's a quick summary of their key differences. Softmax is probabilistic. It gives you a distribution of reactions. UCB is deterministic. Once the UCB scores are calculated, you always pick the max. Softmax's key idea is proportionality to value, while UCB is about optimism in the face of uncertainty. Another crucial difference is how they handle uncertainty. Softmax does it implicitly. Lower value actions are still chosen sometimes. UCB is very explicit about it, with a mathematical term dedicated to modeling and reducing uncertainty. Because of this, UCB is known to have better theoretical guarantees in stationary problems, that is, problems where the reward distributions don't change over time. It can find the best action very efficiently. Softmax being probabilistic can be a bit more robust in non-stationary environments where the best action might suddenly change as it never fully stopped exploring all options. Finally, both have a key parameter to tune, the temperature tau for Softmax and the exploration constant C for UCB. Let's finish up this video by summarizing what we've learned. To recap, we've now added two powerful tools to our exploration toolkit. We moved beyond the naive randomness of epsilon greedy to the more principled approach of softmax, which weights choices by their value, and the mathematically elegant upper confidence bound, which explicitly uses uncertainty to drive exploration. These methods are fantastic when there are rewards to be found. But what happens in environments where rewards are incredibly sparse? Imagine a game where you only get a single point after completing a very long and complex sequence of actions. An agent using UCB or Softmax might wander for a very, very long time without ever stumbling upon that reward, and thus it would never learn anything. To solve this, we need a different kind of motivation, an intrinsic one. As such, in our next video, we will introduce the fascinating concept of curiosity -driven exploration where the agent generates its own reward signal simply by trying to understand its environment. See you there. 

#### LECTURE5

 Welcome back. In our last video, we explored how the Softmax and UCB strategies can make exploration more intelligent by considering the value and uncertainty of actions. However, these methods share a fundamental assumption, that the environment will provide extrinsic rewards to guide the learning process. But what happens when that's not the case? In this video, we tackle one of the most challenging problems in reinforcement learning, the sparse reward problem. We will introduce a powerful new paradigm called curiosity-driven exploration, where the agent learns to generate its own intrinsic reward signal, allowing it to explore and learn meaningfully even when environmental feedback is minimal or non-existent. This is where we teach our agent to be curious. Let's start by defining the challenge, the sparse reward problem. This occurs in environments when an agent can take thousands or even millions of actions without ever receiving a single point of reward from the environment. Think of a complex puzzle game where the only reward comes at the very end when the puzzle is solved. What are the consequences of this? Well, strategies like Epsilon Greedy or UCB are completely paralyzed by this. They rely on rewards to update their Q values and learn. But if there are no rewards, then there is no learning signal. The agent is essentially wandering in the dark with no feedback, and its behavior never improves beyond random exploration. A classic example for this problem is the Atari game Montezuma's Revenge, which you can see here. To get the first reward, the player must navigate a series of ladders, make a precise jump, and grab a key all while avoiding enemies an agent exploring randomly has virtually no chance of ever completing this sequence so how do we get an agent to explore productively in such a world well we need to give it a new source of motivation the solution is to shift our thinking from extrinsic rewards provided by the environment to intrinsic motivation generated by the agent itself. Instead of programming the agent to seek external points, we program it to be curious. But what does curiosity mean for an algorithm? In this context, we define curiosity as the agent's desire to explore and understand its environment. We can formalize this by giving the agent a reward for being surprised, where surprise is defined as the error in the agent's prediction about what will happen next. The more surprising the outcome of the action, the larger the curiosity reward the agent gives itself. This creates a powerful feedback loop. The agent is incentivized to seek out situations where its internal model of the world is inaccurate. By experiencing these surprising states, it gets data to improve its model. Then, as the model improves, those states are no longer surprising, so the agent loses interest and seeks out new, unexplored parts of the environment. It's a self-driven cycle of exploration. This diagram shows the architecture of a popular method called the Intrinsic Curiosity Module, or ICM. It looks complex, but let's break it down. On the left, we have the agent's current state, st, and the next state it observes after taking an action, st plus one. these states are first fed into a feature encoder whose job is to transform the raw state like the pixels of a game screen into a more abstract and useful representation we will see why this is important in a moment the core of the icm consists of two models the forward model tries to predict the feature representation of the next state, given the current state's feature and the action taken. Then, this predicted next state is compared with the features of the actual next state and an error metric is computed. Mean squared error in this case. This error measure becomes our curiosity reward. The total reward fed to the main reinforcement learning algorithm is the sum of the normal extrinsic reward from the environment and this new intrinsic curiosity reward. The inverse model does the opposite. It looks at the features of the current and next state and tries to predict which action was taken to get from one to the other let's look at each of these models more closely the forward model is the engine of our agents curiosity its job is to answer the question given where I am now and the action I'm about to take what What will I see next? It takes the feature vector of the current state, 5 of st, and the action, a t, and predicts the feature vector of the next state, that is, pi hat of st plus 1. The agent then actually takes the action and observes the true next state, getting the feature vector 5 phi of st plus 1. The intrinsic reward is simply the difference between what it thought would happen and what actually happened. As you can see in the formula, we typically use the squared Euclidean distance between the predicted and actual feature vectors. This prediction error, which the model is trying to minimize as its loss function, LF, is repurposed as our reward signal. The bigger the error, the more surprising the outcome, and the higher the curiosity reward. This encourages the agent to seek out parts of the state space where its forward model is weak. By repeatedly visiting these areas, it gathers data. Its predictions improve, the error drops, and it gets bored, moving on to find new sources of surprise. At this point, you might be wondering, what is the role of the inverse model? Given that the forward model favors surprises and unpredictability, the issue becomes, what if some parts of the environment are unpredictable but completely irrelevant to the agent's goal? Think of leaves blowing in the wind in a game or a TV screen showing random static. A simple forward model would find this randomness infinitely surprising and would get a high reward for just sitting and watching it. This is famously known as the noisy TV problem. The solution lies in forcing our feature encoder to learn what actually matters. This is the job of the inverse model. The inverse model is trained on a simple task. Given the features of the state before and after an action, predict which action was taken. In order to solve this task, the feature encoder must learn to represent the aspects of the state that are actually affected by the agent's actions. It will learn to ignore the blowing leaves or the noisy TV, because those things happen regardless of what the agent does. by training both the forward and inverse models together we constrained the feature space to things the agent can control creating a much more robust and meaningful curiosity reward so how does the intrinsic curiosity module fit into the reinforcement learning process process. Let's put it all together in pseudocode. This update function would be called at each step of the learning process. First, we pass the current and next states through our encoder to get their feature representations. Second, we use the forward model to predict the next state's features and calculate our forward loss, which we can then scale to become our intrinsic reward. Third, we combine this with any extrinsic reward from the environment to get the total reward for this step. Fourth, and this is key, we use this total reward to update our main reinforcement learning agent's policy, just like we always would. The agent is learning to take actions that maximize the combination of environmental reward and its own curiosity. Finally, in a separate step, we train the ICM itself. We calculate the inverse loss by predicting the action and comparing it to the real one. And we use both the inverse and forward losses to update the weights of the encoder and the two ICM modules. This entire process allows the agent to simultaneously learn how to explore and how to solve the task. Let's do a quick recap of what we've learned. To summarize, we have seen that the sparse reward problem is a major hurdle for reinforcement learning. The powerful solution is to grant our agents intrinsic motivation in the form of curiosity. We explored the Intrinsic Curiosity module, where a forward model generates a curiosity reward based on prediction error. And a crucial inverse model ensures the agent is curious about things it can actually control, not irrelevant distractions. This family of techniques allows agents to explore vast and complex environments efficiently. learning useful skills even in the complete absence of feedback from the environment. This concludes our lecture on smart exploration strategies. See you in the next video. 

#### LECTURE6

Hello and welcome back. In our previous lectures, we've explored methods like Q-learning, which fall under the umbrella of value-based reinforcement learning. These methods focus on learning the value of being in a certain state or taking a certain action in a state. The policy itself is often implicit. For example, with Q-learning, our policy is to just act greedily with respect to the learned key values. Today, we're going to introduce a fundamentally different approach, policy -based methods. Instead of learning a value function, we will learn a policy directly. This shift in perspective opens up solutions to problems that are difficult, or even impossible, for value -based methods to solve. In this first video, we will cover why we need this new approach and the core concept behind how it works. Before we dive into our new method, let's motivate it by looking at the limitations of the value -based approaches we've seen so far. Remember, in queue learning, our goal is to find the best queue value for every state action pair. Our policy then simply becomes pick the action with the highest queue value. This works well for many problems, but it runs into trouble in several key areas. first consider continuous action spaces imagine controlling a robot arm or the steering wheel of a car there's an infinite number of precise angles you could choose finding the action a that maximizes Q would require an optimization procedure at every single time step which is highly impractical. Second, value-based methods typically produce deterministic policies. For any given state, they will always output the same best action. But some problems require stochastic or random policies to be optimal. The classic example for this is rock paper scissors. If you always play rock, you'll be easily beaten. The optimal policy is to play each action with one -third probability. A simple value-based agent can learn this. Finally, sometimes learning the value function is just harder, more complex problem than learning the policy directly. The value function can be an unnecessary intermediate step. This brings us to our new approach, policy-based reinforcement learning. The idea is elegantly simple. Instead of learning a value function, let's just learn the policy directly. As the diagram shows, we are cutting out the middleman. We want to build a machine that takes a state as input and directly outputs the action, or a probability distribution over actions. To do this, we introduce what's called a parameterized policy. We define our policy, which we call pi, as a function that depends on a set of parameters. which we will denote by the Greek letter theta. This function, pi theta, takes a state S as input and gives us the probability of taking action A. In modern deep reinforcement learning, this policy is almost always a neural network, which we call a policy network. The parameters theta are simply the weights of this neural network. Our goal is to find the best possible set of weights that define the best possible policy. This direct approach immediately solves the problems we identified earlier. For continuous action spaces, we can design our policy network to output the parameters of a probability distribution. For example, it could output a mean and a standard deviation for a Gaussian. We then simply sample an action from that Gaussian distribution. This is a clean and powerful way to handle continuous control. It also naturally learns stochastic policies. Since the output of our network is a probability distribution, it can easily learn to assign non-zero probabilities to multiple actions, solving problems like rock, paper, scissors, where deterministic policies fail. Finally, as we mentioned, we are now focused on a more direct objective. The optimization landscape for the policy can be smoother and easier to navigate than the landscape for a value function, leading to better and more stable convergence in many complex problems. So, if we're going to optimize our policy, we need a clear objective. What makes a policy good? Well, a good policy is one that accumulates a lot of reward. So our objective is to maximize the expected total reward. Let's formalize this. We define a trajectory, which we will call tau, as a full sequence of states and actions from the beginning to the end of an episode. The total reward for that trajectory is just the sum of all the individual rewards received. Our objective function is the expected value of this total reward, where the trajectories are generated by following our current policy, pi theta. In simple terms, this objective function answers the question, if my agent with its current policy parameters theta plays an episode, what is the average total score it will get? Our ultimate goal is to find the optimal parameters theta star that make this average score as high as possible. Okay, so we have our objective function j theta. So how do we maximize it? The answer comes from calculus. We perform gradient ascent. You may be more familiar with gradient descent, which we use to minimize a loss function. The gradient ascent is the same idea, but we move uphill to maximize a function. The process is iterative. At each step, we calculate the gradient of our objective function with respect to our policy parameters theta. This gradient vector points in the direction of the steepest increase in our expected reward. It tells us precisely how to tweak each weight in our policy network to improve our policy. policy. We then update our parameters by taking a small step in that direction, scaled by learning rate alpha. As the figure shows, we are essentially hill -climbing on the surface of our objective function, iteratively taking steps towards the peak, which represents the optimal policy. This, however, leaves us with one huge question. How on earth do we calculate the gradient? Calculating this gradient seems incredibly difficult. The expected reward depends on the distribution of trajectories, which itself depends on the policy and the unknown environment dynamics. Trying to differentiate this whole system directly is not feasible. Fortunately, there is a remarkable result known as the policy gradient theorem that gives us a way out. It provides an alternative expression for the gradient that is much easier easier to work with. Let's focus on the more intuitive practical form shown below. It says we can estimate the gradient by running our policy for n episodes. For each episode, we look at every action we took. The term navla log pi tells us which direction in parameter space would have made that specific action more likely. We then multiply this direction by the total reward we got in that entire episode. If the total reward was high, we push our parameters significantly in that direction. If the total reward was low or negative, we push them in the opposite direction. The core intuition is this. We are reinforcing actions that led to good outcomes. We make the sequences of actions that resulted in high rewards more probable. Let's wrap up this lecture. We've introduced a major new class of reinforcement learning algorithms. We contrasted policy -based methods with the value-based methods we've seen before, highlighting their advantages in continuous and stochastic environments. The core idea is to represent our policy with a set of parameters, and directly optimize those parameters to maximize the expected total reward. We do this using gradient ascent, and the policy gradient theorem gives us a clever way to estimate the required gradient by running our policy and seeing what happens. The intuition is simple and powerful. Reinforce good actions and suppress bad ones. In the next video, we will take these ideas and implement them in our first policy gradient algorithm, a classic method called Reinforce. We will look at its pseudocode and refine the gradient calculation to make it even more effective. See you there. 

#### LECTURE7

Welcome to the second part of our lecture on policy gradient methods. In the last video, we established the core idea. Instead of learning a value function, we can directly learn a parameterized policy pi. We use gradient ascent to update the policy's parameters to maximize the expected total reward. We ended with the policy gradient theorem, which gave us a way to estimate the gradient. In this video, we'll make this concrete. We'll introduce our first policy gradient algorithm, reinforce. We will look at its implementation, identify a major weakness, and then show how to fix it using a powerful technique called a baseline. This will lead us directly to the doorstep of modern state-of-the-art reinforcement learning algorithms. Let's quickly recap the three key ideas from our last video. First, our goal is to directly optimize a policy which is a function, like a neural network, with parameters theta. Second, we do this using gradient ascent, iteratively updating our parameters in the direction that increases our expected reward. Third, the policy gradient theorem gives us a formula to estimate the gradient via sampling. The intuition behind it is the most important part. We want to find out which direction in parameter space would make a good action more likely, and then push our parameters in that direction, weighted by how good the outcome actually was. Now, let's build an algorithm that does exactly this. Our first policy gradient algorithm is called Reinforce. You will also see it referred to as Monte Carlo policy gradient, which gives a hint about how it works. It is the most straightforward implementation of the policy gradient theorem we just discussed. The process is simple and follows a clear loop. We start with a randomly initialized policy network. Then, we use this policy to run a complete episode, from the start state to a terminal state, collecting all states, actions, and rewards along the way. This gives us one trajectory. Once the episode is over, we go back in time. For every single step we took, we calculate the reward to go, which we will call g at time t. This is just the sum of all rewards from that point in time until the end of the episode. This GT will be our estimate of how good it was to be in state ST and take action AT. We then use these returns to perform a single update of our policy network's weights and repeat the whole process, generating a new trajectory with our slightly improved policy. Here's the pseudocode for reinforce. We start by initializing our policy parameters theta, and then we enter the main training loop. Inside the loop, the first thing we do is generate one full episode by interacting with the environment using our current policy. We store all the states, actions, and rewards in the trajectory list. Once the episode is done, we work backwards from the end. We initialize our reward to go g to zero. Then, starting from the last time step, we update g by adding the reward from that step, plus the discounted g from the next step. This way, G always represents the total discounted reward from that point forward. With G calculated for that time step, we perform the core update. We calculate the gradient of the log probability of the action we took, and we update our parameters theta by stepping in that direction, scaled by the learning rate alpha, and crucially by our reward-to-go g. We do this for every step in the episode. While reinforce is theoretically sound, in practice it has a major problem, high variance. The reward-to-go that we use as our weighting factor can be incredibly noisy. Think about it. The exact same policy can produce trajectories with vastly different total rewards, just due to random chance in the environment, or the stochasticity of the policy itself. A lucky streak of good random events can lead to a high reward, while an unlucky streak can lead to a very low one. This creates a huge problem for our gradient estimate. As this graph shows, the learning process becomes very unstable. If we happen to get a lucky trajectory with a high total reward, the algorithm will heavily reinforce every single action taken during that episode, even the ones that were actually mistakes. Conversely, an unlucky trajectory will suppress every action. This noisy signal makes it very difficult for the agent to figure out which actions are consistently good, so learning is slow and unstable. To solve this variance problem, we introduce a simple but incredibly effective idea, a baseline. The insight is that we shouldn't care about the raw score of an episode. If all possible outcomes in a game give a reward between 1000 and 1010, then getting a reward of 1001 is actually a bad outcome, but reinforced would treat it as a huge success. That is, we need to center our rewards. Hence, the introduction of the baseline. We simply subtract a baseline, B of st, from our reward to go gt. This baseline should be an estimate of the average return we expect from a state st. The resulting quantity is called the advantage. The logic is now much more intuitive. If the return we got was better than the baseline, the advantage is positive, and we reinforce the action. If the return was worse than the baseline, the advantage is negative, and we discourage the action, pushing its probability down. This simple change dramatically reduces the variance of updates and stabilizes learning. So, what should we use for our baseline? The most natural and common choice is the state value function, BST. Remember, the value function is defined as the expected future return from a given state. It's the perfect definition of an average performance from that state. When we use the value function as our baseline, our advantage estimate becomes the reward to go minus the value of the state. This quantity tells us how much better or worse taking action AT was compared to the average action from state ST. Our new policy gradient formula is shown on the slide. It's identical to the old one, except we've replaced the noisy reward to go GT with the much more stable advantage estimate A. This raises a new question, though. How do we get the value of function VST? Well, we can learn it, just like we did in our value -based methods, often using a second neural network that is trained alongside the policy network. This leads us to the pseudocode for reinforce with a baseline. It is very similar to before, but with a few key additions. Now we initialize the parameters for two networks, our policy network theta and our value network phi. Then, after we generate a trajectory, we loop through the steps as before and calculate the reward to go gt. But now, before updating our policy, we first get the value estimate for the current state from our value network. We then calculate the advantage by subtracting this value from gt. The policy update is the same as before, but we use the advantage as our weighting factor instead of gt. This is the variance reduction step. Finally, we need to make sure our value network is accurate. So we add one more step. We update the network's parameters phi to make its prediction closer to the actual return. This is typically done by minimizing the mean squared error. This structure, with one network choosing the actions, aka the actor, and another critiquing those actions, aka the critic, is a huge idea in reinforcement learning. At this point, you may be justifiably asking yourself, wasn't one of the key advantages of policy gradient methods over value-based methods the fact that we did not need to compute a state value function? Well, the key difference here lies in the role of the value function. In classic value -based methods like Q -Learning, the value function is everything. The policy is derived from it. The entire goal is to learn an accurate Q-table or Q-network. Action selection is done by finding the action that maximizes the Q-value. If your value estimates are wrong, your policy is wrong. The value function determines the policy. In policy gradient methods with a baseline, the value function's role is completely different. The policy network directly selects the action. The value function is never consulted during the decision -making process. The value function is only used after the fact during the learning update to calculate the advantage. Its sole purpose is to reduce the variance of the gradient updates. making the learning process much faster and much more stable. You could still use the standard reinforced algorithm without learning a value function at all. However, it would learn very slowly due to the high variance problem we discussed. We choose to learn the state value function because it provides a powerful baseline that makes the policy gradient updates much more effective. Let's wrap up this lecture by summarizing what we've learned. To summarize this lecture, we started with the abstract policy gradient theorem and built a concrete algorithm called reinforce. We saw that, while it works in theory, its reliance on the noisy Monte Carlo return suffers from high variance which makes it very unstable. The critical improvement was to introduce a baseline, typically the value function, and use the advantage, that is, the return minus the baseline, as our learning signal. This stabilizes training without introducing bias. This powerful structure where we have a policy network that decides what to do and a value network that critiques those decisions is known as an actor -critic architecture. Actor-critic methods, which build directly on the ideas we've discussed today, are among the most important and widely used algorithms in modern reinforcement learning, and will be the focus of our next lecture. 

#### LECTURE8

Welcome to our final lecture for this week. Last time, we began looking at pure policy -based methods with the reinforced algorithm. Then, to solve its high variance problem, we introduced a value function as a baseline. This led to a powerful hybrid structure. In this video, we will formalize that structure and introduce the actor-critic family of algorithms. These methods combine the best of both value-based and policy-based approaches and form the foundation for many of the most successful deep reinforcement learning agents today. We will see how separating our agent into two distinct roles, an actor and a critic, leads to a more stable and efficient learning. Let's quickly recap our journey. We started with pure value-based methods like Q-learning, which are great but struggle in continuous action spaces. Then we moved to purely policy-based methods like Reinforce, which can handle continuous spaces but are very unstable due to high variance in their gradient estimates. The solution was to combine them. We used a learned value function as a baseline to reduce the variance in our policy updates. This hybrid approach is exactly what actor-critic methods formalize. The actor-critic architecture formalizes this split into two distinct networks. The actor is the policy. Think of it as the component that decides what to do. It takes the current state and outputs an action or a distribution of our actions. Its parameters are our policy weights, theta. The critic is the value function. Think of it as the art critic who doesn't create art but evaluates it. The critic looks at the state and evaluates how good the situation is, or how good the actor's last move was. Its parameters are the value function weights, phi. As the diagram shows, they operate in a loop. The actor selects an action, the environment responds, and then the critic evaluates what happened. The critic then provides a feedback signal that is used to update both its own understanding and, most importantly, the actor's policy. Let's see how that feedback signal is calculated. Instead of waiting until the end of an episode to calculate the Monte Carlo return GT, the actor-critic methods update at every step. To do this, the critic calculates the temporal difference, or TD, error, which you might remember from our lecture on Q-Learning. Remember, the intuition is to compare what I got versus what I expected. The temporal difference formula looks as follows. The TD target represents an updated estimate of the value of state ST. It's the immediate reward we got, plus the discounted value of the state we ended up in. We compare this target to our old estimate, B . The difference between them is the TD error, delta T. This TD error is a fantastic signal. It tells us if we did better or worse than the expected on that single step. It serves as our new low variance estimate for the advantage function. If delta t is positive, the action led to a surprisingly good outcome. If it's negative, the outcome was surprisingly bad. Now, how do we go about using this temporal difference error? Well, first, we use it to train the critic itself. The critic's job is to produce accurate value estimates. If the TD error is large, it means the critic's prediction, B of ST, was wrong. So we want to update the critic's parameters, phi, to make its prediction closer to the TD target. The update rule looks a lot like gradient ascent. We notch the parameters, phi, in the direction of the gradient of the value function. scaled by the TD error itself. If the TD error was positive, we increase the value function. If it was negative, we decrease it. This makes the critic better at its job over time. Next, and most importantly, we update the actor. The actor's goal is to learn to take actions that lead to good outcomes. The critic provides the definition of good via the TD error. If the TD error is positive, it means the chosen action was better than expected. So we should update our policy parameters, theta, to make that action more likely in the future. The update rule is exactly the policy gradient update from our last lecture. But instead of using the high variance Monte Carlo return, gt, we use the low variance TD error, delta t, as our weighting factor this is the heart of the actor critic method we're using a value-based signal to guide our policy based learning here we can see the pseudocode for a standard actor critic algorithm after often called advantage actor critic or A to C it ties everything together as usual we initialize both our actor and critic networks. Then, in our main loop, we don't wait for the episode to end. We update at every single step. First, the actor picks an action and we observe the result from the environment. Then, the critic evaluates what happened by calculating the TD target and the TD error which is our advantage signal. Next we use this TD error to put to update both networks. We update the critics parameters phi to improve its future predictor predictions and we update the actors parameters theta to encourage or discourage the action it just took. Finally we move to the next state and repeat. This step-by-step learning process is much more sample efficient than the episode-by-episode updates of Reinforce. Let's apply this to a concrete example, an agent learning to play Pac-Man. Pause the video while you think about these two questions. Once you have an answer, continue watching to confirm your answers. Okay, so for question one, when Pac-Man moves toward a power pellet, the critic sees this as progress. The value of the next state is higher than the value of the current state. This makes the TD error positive. As a result, the actor's choice to move forward the pellet is reinforced, making it more likely in the future. The Critic's own value for the original state is also nudged upward, as it was apparently a better state than it initially thought. Question 2 is basically the reversed scenario. When Pac-Man moves toward a ghost, the situation is worse. The value of the next state, closer to danger, is much lower. This makes the TD error strongly negative. The policy gradient update will now discourage the action that led towards the ghost, making it less likely in the future. The critic will also revise its value for the original state downward because it led to a bad situation. This is how the agent learns to seek rewards and avoid danger. So, why are actor-critic methods so popular? Well, because they truly give us the best of both worlds. From policy gradients, they inherit the ability to work in continuous action spaces and to learn optimal stochastic policies, while still learning a policy directly. But from value -based methods, they inherit the technique of bootstrapping, that is, using the TD error to provide low-variance learning signals. This makes them much more stable and sample-efficient than a pure Monte Carlo method like Reinforce. This combination of flexibility and stability is why actor-critic framework is so dominant in modern reinforcement learning. To summarize, actor-critic methods elegantly combine a policy network, the actor, with a value function network, the critic. The critic provides a stable, low -variance learning signal in the form of the TD error, and the actor uses this signal to improve its policy. This framework underpins many of the algorithms you'll hear about in research, like A3C, DDPG, and PPO. And with that, we conclude our week on Q-Learning and Policy Gradient Methods. We've built up from the basic idea all the way to a powerful modern architecture. So far, all of our discussions have been about a single agent, a single actor, learning in a world that might be random, but isn't actively strategizing against it. But what happens when we introduce other learning agents into the environment? What if our actor has to compete or cooperate with another actor? This fascinating question leads us directly into our topic for next week, multi-agent systems and the fundamentals of game theory. See you in the next module. 
